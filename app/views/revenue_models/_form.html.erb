<%= form_for(@revenue_model) do |f| %>
  <% if @revenue_model.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@revenue_model.errors.count, "error") %> prohibited this revenue_model from being saved:</h2>

      <ul>
      <% @revenue_model.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
